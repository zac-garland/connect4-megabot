# Python env for megabot (notebooks + src/connect4_model_hub.py)
# Create venv (prefer 3.10+; this repo was validated with Homebrew 3.13):
#   /opt/homebrew/bin/python3.13 -m venv .venv
# Or:  python3 -m venv .venv
# Activate:  source .venv/bin/activate
# Install:   pip install -r requirements.txt

numpy>=1.26,<3
pandas>=2.1,<3
matplotlib>=3.8,<4
tensorflow>=2.15,<2.22

jupyter>=1.1.0
ipykernel>=6.29
